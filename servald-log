#!/bin/bash
source serval-base

mkdir -p $LOG_PATH
OUTFILE="$LOG_PATH/servald-$(date +%s).csv"
echo "timestamp_ms,peer_count" | tee -a $OUTFILE

while [ true ]; do
    sleep_until_next_ms 200
    NOW=$(date_ms)
    COUNT=$(servald peer count)
    echo "$NOW,$COUNT" | tee -a $OUTFILE
done