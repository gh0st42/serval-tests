#!/bin/sh
export SCRIPT_PATH=$(dirname $0)
echo $SCRIPT_PATH

$SCRIPT_PATH/servald-log &
echo $! > $SCRIPT_PATH/servald-log-$(hostname).pid
$SCRIPT_PATH/rhizome-log &
echo $! > $SCRIPT_PATH/rhizome-log-$(hostname).pid
$SCRIPT_PATH/meshms-log &
echo $! > $SCRIPT_PATH/meshms-log-$(hostname).pid
